# Tamagui benchmarks

This repository preserves Tamagui's historical framework benchmarks and hosts the
current cross-platform Tailwind coverage comparison in [`tailwind-coverage/`](./tailwind-coverage).

The coverage project is deliberately independent of the Tamagui monorepo. A clean
clone installs pinned framework versions, regenerates the Tailwind candidate catalog,
and verifies every published report from executable evidence.
